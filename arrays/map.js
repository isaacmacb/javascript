//map(): Cria um novo array com os resultados de aplicar uma função a cada item do array.
let numeros = [1, 2, 3]; 
let quadrados = numeros.map(num => num * num); // Como é multiplicação, entao seria, 1x1 / 2x2 / 3x3
console.log(quadrados); // p resultado é [1, 4, 9]


